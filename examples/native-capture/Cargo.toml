[package]
name = "native-capture-example"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
default-run = "native-capture-example"

[features]
default = []
disabled = ["perfetto-everywhere/disabled"]

[dependencies]
perfetto-everywhere = { path = "../../crates/perfetto-everywhere" }
